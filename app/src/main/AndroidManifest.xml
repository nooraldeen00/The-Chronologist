<?xml version="1.0" encoding="utf-8"?>

<!-- MUST change the first activity name to "com.example.game.MainActivity" -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Logindemo"
        tools:targetApi="31">

        <activity
            android:name="com.example.game.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.noActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name="com.example.game.LevelSelectActivity"
            android:theme="@style/AppTheme.noActionBar">
        </activity>

        <activity
            android:name="com.example.game.GameActivity"
            android:theme="@style/AppTheme.noActionBar">
        </activity>

    </application>

</manifest>